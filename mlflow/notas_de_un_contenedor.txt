Desde terminal, donde está el DockerFile, ejecutar:

docker build -t image_models .

docker run -it --name container_models -p 5000:5000 -v C:/Users/fabri/Documents/Posgrado/AMq2/TP_Final/mlflow/experiments/models/mlruns:/app/mlruns image_models

cd hasta RegLog.py (será dentro del contenedor)

python RegLog.py (y los scripts que sean)

mlflow ui --host 0.0.0.0 --port 5000 &

Desde navegador web:

http://localhost:5000

